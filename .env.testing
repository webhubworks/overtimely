# Loaded instead of .env whenever APP_ENV=testing, which phpunit.xml.dist sets for the
# test suite. It deliberately defines none of the TIMELY_* or DISPLAY_* variables, so
# tests never inherit a developer's real credentials or display settings and every
# setting starts out at its built-in default.
